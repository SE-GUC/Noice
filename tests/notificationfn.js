// This file is commented as notifications is a deleted entity
// This code is from an old commit where the branch was deleted
// and the commit is lost
/*************************************************************/

/*
const axios = require('axios');

const functions = {
    viewNotification: async () => {
        const notification = await axios.get(`localhost:3000/api/notifications/}`)
        return notification
    },

    viewNotificationByID: async (id) => {
        const notification = await axios.get(`http://localhost:3000/api/notifications/${id}`)
        return notification
    },

    createNotification:async (req) => {
        const notification = await axios.post('http://localhost:3000/api/notifications/',req)
        return notification
    },

    updateNotification: async (id,req) => {
        const notification = await axios.put(`http://localhost:3000/api/notifications/${id}`,req)
        return notification
    },

    deleteNotification: async (id) => {
        const notification = await axios.delete(`http://localhost:3000/api/notifications/${id}`)
        return notification
    }
};
     
                
        
    module.exports = functions;
    */
        